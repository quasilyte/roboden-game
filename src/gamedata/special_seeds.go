package gamedata

var SpecialSeedMap = map[int64]bool{
	// A very tough seed that unlocks the achievement.
	// Effects:
	// * +8 ion mortars on the map
	// * Every air base has a crawlers factory nearby
	// * x2 red crystals on the map
	1337: true,
}
